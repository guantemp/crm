package crm.hoprxi.adapter.weixin;

//import okhttp3.ResponseBody;
//import retrofit2.Call;
//import retrofit2.http.*;


public interface WeixinCard {
/*
    @GET("token")
    Call<Token> token(@Query("grant_type") String grant_type
            , @Query("appid") String appid, @Query("secret") String secret);

    @POST("media/uploadimg")
    @Multipart
    Call<ResponseBody> uploadimg(@Query("access_token") String access_token);

    @POST("card/create")
    Call<ResponseBody> createCard(@Query("access_token") String access_token, @Body Card card);
    */
}
